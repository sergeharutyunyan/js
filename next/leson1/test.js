"use strict";
const a = 5;
let c = 4;
let b = "ss";
let f = true;
let d = ["sdf", "ddd",];
let e = 3;
e = "ss";
function test(a) {
    return "";
}
/**
 *
 * @param a
 */
const test2 = (a) => {
    return a * 2;
};
d = d.map((x) => x.toLowerCase());
/**
 *
 * @param cord
 */
function countCord(cord) {
}
function printIt(id) {
    if (typeof id === 'number') {
        console.log(id.toString());
    }
    else if (typeof id === 'string') {
        id.toUpperCase();
    }
}
function getSum(a) {
    if (Array.isArray(a)) {
    }
}
const aa = undefined;
const z = null;
